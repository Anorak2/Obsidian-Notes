
[[ServiceNow]]

UI policies are a way for a database admin to make certain field read only or optional or required or invisible etc, and Data Policies are the usual [[Database Bibliography|database]] idea of restricting what users can put into the database to ensure valid data.

A UI action is just anything that users are able to click inside of the platform


**Client Scripts**
These are a lot of what they sound like, they run client side and require JS. It can include things as simple as throwing up a confirm box or changing a couple of values