
[[Discrete math Bibliography]]
A power set is the set of all subsets of a set A, which is denoted by $\mathcal{P}(A)$ 

for example if A = {a,b} then
$$\mathcal{P}(A) = \{\varnothing, \{a\}, \{b\}, \{a,b\}\}$$
the power set will also always have $2^n$ number of sets