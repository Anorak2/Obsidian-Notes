
[[Theory of Computing]]

A recursive language is a language that has an associated [[Turing Machines|Turing Machine]] that decides all input strings. It may hang (run forever) when it is given an input string that is not in the language.